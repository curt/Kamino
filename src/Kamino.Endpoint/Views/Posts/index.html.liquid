<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Posts</title>
</head>
<body>
    <section>
        <h1>Posts</h1>
        {% for post in Model -%}
        <article>
            <h2><a href={{ post.Uri }}>{{ post.Title }}</a></h2>
            <p>
                <em>{{ post.PublishedAt | date: '%+' }}</em> &mdash;
                <span>{{ post.Summary }}</span>
            </p>
        </article>
        {%- endfor %}
    </section>
</body>
</html>
